{
  "name": "gemini",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Gemini AMPM — Astro port of the prototype at ../gemini-prototype-src/. Same data, same CSS, same components, same copy — just server-rendered at real URLs.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.18"
  }
}
